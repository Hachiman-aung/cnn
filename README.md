# Fruits360 CNN Classification

A Convolutional Neural Network (CNN) implementation for fruit recognition using the Fruits360 dataset. This project includes a pre-trained model for immediate use.

## 📋 Prerequisites

- Python 3.x
- pip package manager

## 🚀 Quick Start

### 1. Clone the repository
```bash
git clone https://github.com/Hachiman-aung/cnn
cd cnn

# Create virtual environment
python -m venv venv

# Activate virtual environment
# On Windows:
venv\Scripts\activate
# On MacOS/Linux:
source venv/bin/activate

# Install required packages
pip install -r requirements.txt
 
